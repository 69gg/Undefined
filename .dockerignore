# Docker 构建上下文瘦身（仓库根）。
#
# 供 `docker build -f src/Undefined/deploy/templates/Dockerfile.bot .` 使用；
# 镜像只需要 pyproject.toml / uv.lock / src / res / img / config / README.md。
# 不排除 deploy/，以便本地调试时按需沿用部署产物（模板自带一份同样的
# 忽略规则，用于只复制模板文件的场景）。

.git
.gitmodules
.github
.githooks
.claude
.codex
.vscode
.idea

# 运行态与产物
data
logs
dist
.cache
.coverage
.pytest_cache
.mypy_cache
.ruff_cache
htmlcov
coverage

# Python 环境
.venv
venv
__pycache__
*.pyc
*.pyo
*.pyd
*.so

# 前端与原生 App
node_modules
apps/**/node_modules
apps/**/dist
apps/**/src-tauri/target
apps/**/src-tauri/gen
apps/**/src-tauri/.cargo

# 文档（镜像不需要；README.md 由 Dockerfile 显式 COPY）
docs
*.md

# NagaAgent 子模块（体积大，container 模式按需 bind mount）
code

# 本地私有文件
config.toml
config/mcp.json
config/prompts/*.local.*
.env
*.jks
*.keystore
keystore.properties
local.properties
knowledge/*/chroma/
knowledge/*/.manifest.json
