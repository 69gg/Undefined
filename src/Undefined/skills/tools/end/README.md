# end 工具

用于结束对话，通常在完成回复后调用一次。

关键信息：
- `action_summary`（可选）：记录 AI 本轮做了什么，建议短句
- `new_info`（可选）：字符串数组，从当前消息提取的新记忆列表（每条一个要点，可多条）
- `force`（可选）：`true` 时可跳过“本轮未发送消息”的结束检查；同时在认知史官绝对化正则闸门失败时允许强制入库
- 两者都可为空；为空时仅结束会话，不写认知队列

目录结构：
- `config.json`：工具定义
- `handler.py`：执行逻辑
