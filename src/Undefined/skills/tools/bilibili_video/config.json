{
    "type": "function",
    "function": {
        "name": "bilibili_video",
        "description": "处理 Bilibili 视频。默认发送到群聊或私聊；可设置 output_mode=uid 只注册视频附件 UID，或 output_mode=info 只获取视频信息。支持 BV号、AV号或B站视频链接。",
        "parameters": {
            "type": "object",
            "properties": {
                "video_id": {
                    "type": "string",
                    "description": "Bilibili 视频标识：BV号（如 BV1xx411c7mD）、AV号（如 av12345）或完整URL"
                },
                "target_type": {
                    "type": "string",
                    "enum": ["group", "private"],
                    "description": "可选。目标会话类型"
                },
                "target_id": {
                    "type": "integer",
                    "description": "可选。目标会话 ID"
                },
                "output_mode": {
                    "type": "string",
                    "enum": ["send", "uid", "info"],
                    "description": "输出模式：send（默认，发送视频合并转发）、uid（仅下载并注册视频附件 UID，不发送消息）或 info（仅返回视频信息，不下载不发送）"
                }
            },
            "required": ["video_id"]
        }
    }
}
