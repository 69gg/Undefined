# /profile 命令说明

## 功能
查看用户或群聊的认知侧写。侧写由系统根据聊天历史自动生成和更新。

## 用法
- `/profile` — 查看自己的用户侧写（默认合并转发）
- `/p -r` — 查看自己的侧写（渲染为图片）
- `/p -t` — 查看自己的侧写（直接文本）
- `/me` — 同上（别名）
- `/profile g` — 查看当前群聊侧写（仅群聊可用）
- `/p g` — 同上
- `/p 123456` — 🔒 超管：查看指定QQ号用户的侧写
- `/p g 789012` — 🔒 超管：查看指定群号的侧写

## 说明
- 私聊只能查看用户侧写，不支持 `g` 参数。
- 群聊默认使用合并转发模式输出，可用 `-t` 直接文本发送，`-r` 渲染为图片。
- 超级管理员可通过传入 QQ 号或群号查看任意用户/群聊的侧写。
- 非超管尝试指定目标ID时会提示权限不足。
