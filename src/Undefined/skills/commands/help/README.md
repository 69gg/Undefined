# /help 命令说明

## 功能
查看命令列表，或查看某个命令的详细帮助。

## 用法
- `/help`
- `/help <command>`

## 示例
- `/help`
- `/help stats`
- `/help /lsfaq`

## 说明
- `<command>` 支持带或不带 `/` 前缀。
- `<command>` 支持命令别名（若该命令配置了别名）。
- 旧参数 `--help` 已弃用，请统一使用 `/help <command>`。
