{
    "type": "function",
    "function": {
        "name": "add",
        "description": "保存一条手动长期记忆（memory 系列）。用于长期稳定、可人工维护的事实（偏好、长期约定、固定配置等）。不用于回忆历史经过；回忆请用 cognitive.* 查询，当前轮新观察请写 end.observations。就事论事，就人论人，不做会话隔离。上限500条，超出上限会自动移除最旧的记忆。",
        "parameters": {
            "type": "object",
            "properties": {
                "fact": {
                    "type": "string",
                    "description": "要记住的内容（简短、具体、有价值的信息）"
                }
            },
            "required": ["fact"]
        }
    }
}
