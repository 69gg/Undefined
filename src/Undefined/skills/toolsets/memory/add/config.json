{
    "type": "function",
    "function": {
        "name": "add",
        "description": "添加一条置顶备忘（memory 系列）。仅用于需要每轮都提醒自己的自我约束或待办（如'用户要求以后用英文回复'、'下周三前完成数据迁移'）。用户事实（偏好、身份、习惯等）不要写这里，一律通过 end.observations 写入认知记忆。回忆历史请用 cognitive.* 查询。上限500条，超出自动移除最旧条目。",
        "parameters": {
            "type": "object",
            "properties": {
                "fact": {
                    "type": "string",
                    "description": "要记住的内容（简短、具体、有价值的信息）"
                }
            },
            "required": ["fact"]
        }
    }
}
