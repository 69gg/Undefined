{
    "type": "function",
    "function": {
        "name": "delete",
        "description": "根据 UUID 删除一条手动长期记忆（memory 系列）。仅作用于 memory.* 清单，不作用于认知记忆事件/侧写。",
        "parameters": {
            "type": "object",
            "properties": {
                "uuid": {
                    "type": "string",
                    "description": "要删除的记忆的 UUID"
                }
            },
            "required": [
                "uuid"
            ]
        }
    }
}
