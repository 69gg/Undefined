{
    "type": "function",
    "function": {
        "name": "get",
        "description": "获取当前发送者可用的某条斜杠命令的详细说明，包括用法、示例、别名、权限（谁可用）、作用域、限流和帮助文档。命令名支持别名。无权或不存在时不返回文档。",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "命令名或别名，可带或不带前导 /，如 profile 或 /p"
                }
            },
            "required": ["name"]
        }
    }
}
